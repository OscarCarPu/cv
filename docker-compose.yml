services:
  cv:
    image: nginx:alpine
    container_name: cv
    restart: unless-stopped
    ports:
      - "8090:80"
    volumes:
      - ./public:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
